import javafx.application.Application;
import javafx.stage.Stage;
import javafx.scene.paint.Color;
public class test{



	public static void main (String[] args){
		Field.setUp();



		
		
		
		/*
		TheSnake snek = new TheSnake(4,4);
		snek.extend(4,5);
	
		
		int[][] pathTest = snek.getPath();
		for (int i =0; i < pathTest.length; i++ ){
			System.out.println((i+1)+": "+pathTest[i][0]+", "+pathTest[i][1]);	
		}
		
		snek.shorten();
		snek.shorten();
		pathTest = snek.getPath();
		for (int i =0; i < pathTest.length; i++ ){
			System.out.println((i+1)+": "+pathTest[i][0]+", "+pathTest[i][1]);	
		}
		
		snek.extend(5,5);	
				pathTest = snek.getPath();
		for (int i =0; i < pathTest.length; i++ ){
			System.out.println((i+1)+": "+pathTest[i][0]+", "+pathTest[i][1]);	
		}
		
		System.out.println(snek.bitesItself(5,5));
		*/
	}
}
